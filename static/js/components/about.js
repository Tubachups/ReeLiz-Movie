// document.addEventListener('scroll', () => {
//   const features = document.querySelector('.features-container');
//   const scrollY = window.scrollY;
//   const windowHeight = window.innerHeight;

//   // when user scrolls down, move the features container up
//   if (scrollY <= windowHeight) {
//     features.style.transform = `translateY(-${scrollY}px)`;
//   } else {
//     // stop moving after fully overlapping
//     features.style.transform = `translateY(-${windowHeight}px)`;
//   }
// });
